<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hts.market.domain.board.repo.BrdImgRepo">



    <insert id="save">
        <selectKey keyProperty="imgNo" order="BEFORE" resultType="long">
            select nvl(max(img_no),0)+1 from board_image where brd_no=#{brdNo}
        </selectKey>
        insert into
            board_image( brd_no, img_no, img_path )

        values( #{brdNo}, #{imgNo}, #{imgPath})
    </insert>




    <!-- 제목, 내용, 조회수, 좋아요, 싫어요, 댓글수 중 사용자가 선택한 컬럼을 변경 -->
    <update id="update">

        <![CDATA[
            UPDATE
                board_Image
            SET
                img_path=#{imgPath}
            WHERE
                 brd_no=#{brdNo} and img_no=#{imgNo}
        ]]>


    </update>


    <delete id="delete">
        <![CDATA[
            DELETE FROM
                board_Image
            WHERE
                brd_no=#{brdNo} and img_no=#{imgNo}
        ]]>
    </delete>
    <!-- 대표이미지 보기 -->
    <select id="findMainImg" resultType="string">
        select img_path from BOARD_IMAGE where brd_no=#{brdNo} and img_no=1
    </select>

    <!-- 이미지 전체보기  -->
    <select id="findAllByBrdNo" resultType="com.hts.market.domain.board.dto.BrdImgDto$Read">
        select brd_no, img_no, img_path from BOARD_IMAGE where brd_no=#{brdNo}
    </select>


</mapper>
