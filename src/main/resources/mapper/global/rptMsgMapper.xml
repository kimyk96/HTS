<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hts.market.global.repo.RptMsgRepo">
    <!-- 메세지 목록 조회-->
    <select id="findAll" resultType="com.hts.market.global.dto.RptMsgDto">
        SELECT
            *
        FROM
            REPORT_MESSAGE
    </select>
</mapper>