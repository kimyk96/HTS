<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hts.market.domain.product.repo.PdtRepo">
    <!-- 판매글등록 -->
    <insert id="save">
        <selectKey resultType="long" order="BEFORE" keyProperty="pdtNo">
            select nvl(max(pdt_no),0)+1 from Product
        </selectKey>
        insert into
        PRODUCT( pdt_no, pdt_cate_no, mem_no, pdt_address_no, pdt_name, pdt_status, pdt_desc, pdt_price,
        pdt_views, pdt_seller_no, pdt_created_at)
        values
        ( #{pdtNo}, #{pdtCateNo}, #{memNo}, #{pdtAddressNo}, #{pdtName}, #{pdtStatus}, #{pdtDesc}, #{pdtPrice},
        #{pdtViews}, #{pdtSellerNo}, #{pdtCreatedAt} )
    </insert>
    <!-- 판매글수정 -->
    <!-- 조회수 증가 -->
    <!-- 판매글 삭제 -->
    <!-- 판매글 보기 -->
    <!-- 판매글 목록보기 -->
    <!-- 제목+내용으로 검색 -->
    <!-- 판매자명으로 검색 -->
    <!-- 카테고리 검색 -->
</mapper>