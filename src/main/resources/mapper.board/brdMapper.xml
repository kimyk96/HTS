<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hts.market.domain.board.repo.BrdRepo">



    <insert id="save">
        <selectKey keyProperty="brdNo" order="BEFORE" resultType="Long">
            select nvl(max(brd_no),0)+1 from BOARD
        </selectKey>
        insert into board(brd_no,brd_mem_no,brd_title,brd_content,brd_cate)

        values(#{brdNo},#{brdMemNo}#{brdTitle}#{brdContent}#{brdCate})
    </insert>




    <!-- 제목, 내용, 조회수, 좋아요, 싫어요, 댓글수 중 사용자가 선택한 컬럼을 변경 -->
    <update id="update">


        <![CDATA[
            UPDATE
                BOARD
            SET
                brd_title=#{brdTitle}, brd_content=#{brdContent}, brd_cate_no=#{brdCateNo},
            WHERE
                 brd_no=#{brdNo}
        ]]>

    </update>


    <delete id="delete">
        <![CDATA[
            DELETE FROM
                BOARD
            WHERE
                brd_mem_no=#{brdMemNoNo} and brd_no=#{brdNo}
        ]]>
    </delete>

    <select id="findByBrdNo()" resultType="com.example.demo.dto.BoardDto$Read">
        <![CDATA[
  	select * from board where brd_no=#{brdNo} and rownum<=1
  	]]>
    </select>

    <select id="findViewsByBrdNo" resultType="Long">
        select brdViews from board where brd_no=#{brdNo} and rownum&lt;=1
    </select>

    <select id="findCreatedAtByBrdNo" resultType="LocalDateTime">
        select brdCreatedAt from board where brd_no=#{brdNo} and rownum&lt;=1
    </select>

    <select id="findCateNoByBrdNo" resultType="Long">
        select brdCateNo from board where brd_no=#{brdNo} and rownum&lt;=1
    </select>
</mapper>
