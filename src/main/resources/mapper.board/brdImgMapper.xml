<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hts.market.domain.board.repo.BrdImgRepo">



    <insert id="save">
        <selectKey keyProperty="brdNo" order="BEFORE" resultType="Long">
            select nvl(max(brd_img_no),0)+1 from board_image
        </selectKey>
        insert into board(brd_img_no)

        values(#{brdImgNo})
    </insert>




    <!-- 제목, 내용, 조회수, 좋아요, 싫어요, 댓글수 중 사용자가 선택한 컬럼을 변경 -->
    <update id="update">

        <![CDATA[
            UPDATE
                board_Image
            SET
                brd_img_no=#{brdImgNo}, brd_no=#{brdNo}, brd_mem_no=#{brdMemNo},
            WHERE
                 brd_no=#{brdNo}
        ]]>


    </update>


    <delete id="delete">
        <![CDATA[
            DELETE FROM
                board_Image
            WHERE
                brd_mem_no=#{brdMemNoNo} and brd_no=#{brdNo}
        ]]>
    </delete>

    <select id="findAllByBrdNo" resultType="Long">
        <![CDATA[
  	select * from board_Image where brd_no=#{brdNo} and rownum<=1
  	]]>
    </select>

</mapper>
